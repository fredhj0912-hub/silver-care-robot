# 파이 당일 실행 순서 (런북)

라즈베리파이 5 앞에서 **위에서부터 그대로 따라가는** 문서다.
배경·이유·되돌리기는 여기 적지 않는다 — `docs/deploy-raspberry-pi.md`(레퍼런스)에 있다.

각 단계 머리의 라벨이 **어느 기기에서 치는 명령인지**를 뜻한다:

| 라벨 | 기기 |
|---|---|
| `[개발 PC]` | 윈도우 노트북 (레포가 있는 곳) |
| `[파이]` | 라즈베리파이 5의 터미널 |
| `[폰]` | 보호자 앱을 여는 휴대전화 |
| `[EC2]` | 개발 PC에서 SSH로 들어간 서버 |

> **✋ 이 문서는 아직 실측이 아니다.** 09-01에 실행하면서 틀린 것을 고칠 것.
> 각 단계의 **"성공한 모습"** 을 보고 넘어가고, 안 되면 그 자리에서 §10을 본다.

---

## 0. 시작 전 — 서버가 살아 있는지 확인 `[개발 PC]`

파이는 EC2가 서빙하는 화면을 여는 브라우저일 뿐이다. **서버 주소부터 확보한다.**

```bash
cd <레포 폴더>
npm run access -- <EC2 퍼블릭 IP>
```

**성공한 모습**: `ACCESS.html 갱신 완료`와 함께 두 주소가 출력된다.
```
📺 라즈베리파이 : https://<터널 주소>/
📱 휴대전화     : https://<터널 주소>/guardian
상태: 백엔드 active / 터널 active / 헬스체크 ok
```

이 **터널 주소를 적어 둔다.** 3번에서 파이에 입력한다.

- 접속 실패 → 인스턴스를 stop/start해서 **퍼블릭 IP가 바뀐 것**이다(reboot은 안 바뀜).
  AWS 콘솔에서 새 IP를 확인해 다시 실행한다.
- `상태: ... inactive` → EC2에 SSH로 들어가 `sudo systemctl start hyodol cloudflared`.
- **터널 주소가 어제와 다르면 폰의 푸시 구독을 다시 해야 한다** — 브라우저는 다른 주소를
  다른 사이트로 취급한다. §8에서 처리한다.

---

## 1. 파이 준비 `[파이]`

파이가 인터넷에 연결된 상태에서 시작한다(clone을 해야 하므로).

```bash
sudo apt update
sudo apt install -y chromium-browser git curl alsa-utils
git clone -b feat/pi-deployment https://github.com/fredhj0912-hub/silver-care-robot.git
cd silver-care-robot/deploy/pi && chmod +x *.sh
```

> ⚠️ **`-b feat/pi-deployment`를 빠뜨리지 말 것.** 스크립트는 이 브랜치에만 있고
> `main`에는 아직 없다. 그냥 `git clone`하면 `deploy/pi` 폴더 자체가 없어서
> 다음 단계가 통째로 막힌다.
> (이 브랜치를 main에 머지한 뒤에는 `-b` 없이 받으면 된다.)

**node도 npm도 설치하지 않는다.** 파이에서는 아무것도 빌드하지 않는다.

**성공한 모습**: `ls`에 `kiosk.sh install-autostart.sh set-url.sh preflight.sh wifi-setup.sh`가 보인다.

---

## 2. 네트워크 등록 `[파이]`

지금 연결된 곳 말고도 **옮겨 다닐 곳을 미리 등록**한다.

```bash
./wifi-setup.sh
```

동아리방 / 행사장 / **폰 핫스팟** 순으로 물어본다. 비밀번호는 화면에 안 보인다.
핫스팟 우선순위가 가장 높은 것은 의도적이다 — 행사장 와이파이가 캡티브 포털로 막혔을 때
**폰에서 핫스팟만 켜면 복구**되게 하려는 것이다.

**성공한 모습**: 마지막에 등록된 연결이 우선순위 순으로 출력된다.

건너뛰어도 된다(지금 연결만으로 오늘 작업은 가능). 다만 **행사 전에는 반드시** 해 둘 것.

---

## 3. 설치 + 주소 입력 `[파이]`

```bash
./install-autostart.sh
./set-url.sh https://<0번에서 적어 둔 터널 주소>
```

`install-autostart.sh`는 세 가지를 한다 — 설정 파일 생성, 부팅 시 자동실행 등록,
화면 꺼짐 해제. **무엇을 했는지 전부 출력**하므로 읽고 넘어간다.

**성공한 모습**: `set-url.sh`가 `✅ .../api/health → 200`을 찍는다.
- `→ 000` 또는 그 외 → 주소가 틀렸거나 EC2가 죽었다. **0번으로 돌아간다.**
- "Chromium이 두 번 뜬다"는 경고가 보이면 §10을 본다.

---

## 4. 사전 점검 `[파이]`

```bash
./preflight.sh
```

기기 → 그래픽 세션 → 네트워크 → **백엔드 연결** → 스피커 → 마이크 → 카메라 → Chromium
순으로 확인한다. 중간에 스피커에서 440Hz 소리가 나므로 **귀로 확인**한다.

**성공한 모습**: 마지막 줄이 `자동 점검 통과`.

**❌가 하나라도 있으면 여기서 멈추고 고친다.** 앱을 의심하기 전에 하드웨어부터다.
가장 자주 틀리는 것은 4번(백엔드 연결) — 터널 주소가 낡은 경우다.

> 6번(마이크)에서 출력되는 **기본 장치 이름**을 꼭 본다. Chromium은 권한 대화상자를
> 자동 수락하면서 **기본 장치**를 말없이 집는다. USB 마이크가 기본이 아니면
> 소리를 못 듣는데도 화면에는 아무 오류가 안 뜬다.

---

## 5. 자동실행 확인 `[파이]`

```bash
sudo reboot
```

**자동실행이 되는지 확인하는 유일한 진짜 방법이다.** 30~60초 기다린다.

**성공한 모습**: 로그인하면 곧 로봇 얼굴이 **전체 화면**으로 뜨고 **마우스 커서가 안 보인다.**

안 뜨면 → §10의 "자동실행이 안 됨".

---

## 6. 화면 · 소리 · 음성 `[파이]` `[폰]`

**순서가 중요하다.** 소리를 먼저 확인해야 음성 실패의 원인을 좁힐 수 있다.

**6-1. 화면** — 얼굴이 뜨고 800×480에 맞게 들어갔는가. 얼굴 아래 **상태 줄**을 읽는다.

**6-2. 소리** `[폰]` — 폰에서 `https://<터널 주소>/guardian/send` 를 열고 아무 말이나 보낸다.

**성공한 모습**: 파이 스피커에서 그 말이 나오고, 화면에 `보호자님 메시지: …`가 뜬다.

**6-3. 음성 인식** `[파이]` — 파이 앞에서 **"효돌아"** 하고 부른다.

**성공한 모습**: 상태 줄이 `말씀하세요, 듣고 있어요`로 바뀌고 로봇이 대답한다.

**상태 줄이 곧 진단 메시지다:**

| 화면 문구 | 뜻 | 조치 |
|---|---|---|
| `"효돌아" 하고 불러주세요` | 정상 | 다음으로 |
| `안전하지 않은 주소로 열렸어요 (HTTPS 필요)` | http로 열렸다 | `./set-url.sh`로 https 주소를 넣는다 |
| `마이크를 쓸 수 없어요` | 권한/장치 문제 | §4의 6번을 다시 본다 |
| `음성 인식 서버에 닿지 않아요` | **Chromium STT 실패** | §10 참고. 텍스트 입력으로 진행 가능 |
| `아래에 글로 말씀해 주세요` | 브라우저가 음성 API 자체를 지원 안 함 | 위와 같음 |

> 🔴 **`음성 인식 서버에 닿지 않아요`가 오늘 가장 가능성 높은 실패다.** 파이 OS 저장소의
> Chromium이 구글 음성 키 없이 빌드돼 있을 수 있다. 그래도 **시연은 가능하다** —
> 화면 아래 텍스트 입력으로 대화가 된다. 여기서 시간을 오래 쓰지 말고 넘어갈 것.

---

## 7. 원격조종 `[폰]` `[파이]`

> ⚠️ **반드시 §8(낙상)보다 먼저 한다.** 응급 상태에서는 원격조종이 잠기고(`423`),
> "원격조종이 고장났다"로 오해하기 쉽다.

폰에서 `https://<터널 주소>/guardian/control` 을 열고 화살표를 누른다.

**성공한 모습**: 파이 화면 위쪽에 `⬅️ 이동 중` 같은 표시가 1.5초 뜬다.

**여기까지가 소프트웨어다.** 실제로 바퀴가 도는 것은 §9의 구동부 프로세스가 있어야 한다.
지금은 화살표가 뜨는 것까지가 정상이다.

**같이 재 둘 것**: 화살표를 누르고 화면에 뜨기까지 **몇 초 걸리는지**. 키오스크 폴링이
2.5초라 그만큼 밀린다. 실물 구동부에는 이 지연이 너무 크다 — 여기서 잰 값이
`services/motion.js`의 데드맨(500ms) 정책을 정하는 근거가 된다.

---

## 8. 카메라 + 낙상 알림 배관 `[파이]` `[EC2]` `[폰]`

**8-1. 카메라 인식** `[파이]` — 화면 오른쪽 위를 본다.

**성공한 모습**: `📷 카메라 없음` 칩이 **안 보인다.**
보이면 §4의 7번(카메라)을 다시 확인한다.

**8-2. 화면 확인** `[폰]` — `https://<터널 주소>/guardian/live`

**성공한 모습**: 파이 카메라가 찍은 최근 사진이 보인다(15초마다 갱신).

**8-3. 푸시 구독** `[폰]` — 보호자 앱 홈에서 **"응급 알림 받기"** 배너가 보이면 눌러 허용한다.
**터널 주소가 바뀌었다면 이 단계가 반드시 필요하다.**

**8-4. 낙상 알림 배관** `[EC2]` — 개발 PC에서 SSH로 들어가:

```bash
cd ~/silver-care-robot/backend
npm run mock-detector -- --type fall --confidence 0.92
```

**성공한 모습**: `🚨 알림 생성됨` → **폰에 푸시 알림 도착** → 파이 화면이 빨갛게 비상 모드.

> 이건 **모델이 아니라 배관을 확인하는 것**이다. 실제 YOLOv8 감지기는 아직 없다
> (`docs/fall-detection.md`). 이 경로가 살아 있으면, 남은 일은 계약대로
> `POST /api/detections`를 쏘는 detector를 만드는 것뿐이다.

**8-5. 알림 해제** `[폰]` — 알림을 눌러 상세로 들어가 **"확인했어요"**.
해제하지 않으면 원격조종이 계속 잠겨 있다.

---

## 9. 구동부 확인 `[파이]`

**정직하게 말하면, 이 레포에는 모터 제어 코드가 없다.** `backend/src/services/motion.js`는
메모리에 가상 좌표만 유지하는 시뮬레이터다. "예전에 움직인 적 있다"는 그때 쓰던 **별도 코드**다.

그래서 오늘 할 일은 **무엇이 어떻게 붙어 있는지 파악하는 것**까지다.

```bash
pinctrl get                 # Bookworm (예전 raspi-gpio 대체)
gpioinfo 2>/dev/null | head -40
ls /dev/i2c-* 2>/dev/null
i2cdetect -y 1 2>/dev/null  # 모터 HAT이 I2C면 여기 주소가 뜬다
```

확인할 것:
- [ ] 구동부가 파이 GPIO/I2C에 **물리적으로 연결돼 있는가**
- [ ] 예전에 쓰던 제어 코드/라이브러리가 어디 있는가 (다른 레포? SD카드? 팀원?)
- [ ] 모터 드라이버 종류 (L298N / 모터 HAT / 서보 등)

**다음 단계의 방향만 적어 둔다** (오늘 만들지 않는다):
찾은 제어 코드를 `GET /api/commands/pending?kind=move&maxAgeMs=2000` 를 폴링해서
실행하고 `POST /api/commands/:id/ack` 하는 프로세스로 감싸면 된다.
**`move`를 ack하는 것은 이 프로세스 하나뿐이어야 한다** — 키오스크는 보기만 하도록
이미 바꿔 두었다.

---

## 10. 막히면

| 증상 | 원인 | 조치 |
|---|---|---|
| `deploy/pi` 폴더가 없다 | `-b feat/pi-deployment` 없이 clone함 | `git checkout feat/pi-deployment` |
| `set-url.sh`가 `→ 000` | 터널 주소가 낡음 / EC2 죽음 | §0으로 |
| 부팅해도 키오스크가 안 뜸 | 컴포지터가 자동실행을 안 읽음 | `./preflight.sh` 2번에서 컴포지터 확인 → 수동 실행 `./kiosk.sh` 로 먼저 화면이 뜨는지 본다 |
| Chromium이 두 개 뜸 | XDG와 labwc 양쪽에 등록됨 | `~/.config/labwc/autostart`에서 hyodol 줄을 지운다 |
| 로봇이 말을 안 함 | 자동재생 차단 / 스피커 기본 장치 | `./preflight.sh` 5번, 그리고 화면을 한 번 터치해 본다 |
| 음성 인식이 안 됨 | Chromium STT 실패 가능성 | 텍스트 입력으로 진행. 시간 쓰지 말 것 |
| 원격조종이 안 먹음 (`423`) | **응급 상태로 잠김** | 폰에서 알림을 해제한다 (§8-5) |
| 인터넷은 되는데 화면이 안 열림 | 캡티브 포털 | 폰 핫스팟을 켠다 |
| 파이 IP를 몰라 SSH 못 함 | 네트워크마다 IP가 바뀜 | `ssh <사용자>@hyodol.local` (mDNS) |

되돌리기는 `docs/deploy-raspberry-pi.md` §7.

---

## 11. 끝나고 `[개발 PC]`

- [ ] **이 문서와 `deploy-raspberry-pi.md`를 실측값으로 고친다.**
      틀린 것을 찾았으면 그게 오늘의 성과다 — 특히 "✋ 실측이 아니다" 문구를 지울 수 있는지
- [ ] §7에서 잰 **원격조종 지연**을 `TODO.md`의 데드맨 정책 항목에 기록
- [ ] `TODO.md`의 "내일 파이에서 확인할 것" 체크박스 정리
- [ ] 검증이 끝났으면 `feat/pi-deployment` → PR → `main` 머지
