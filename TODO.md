# TODO

효돌이 프로젝트 작업 목록. Claude Code가 이 파일을 읽고 다음 작업을 이어간다.

**사용법**
- 작업 시작: "TODO.md 읽고 첫 번째 항목부터 시작해"
- 세션 종료: "TODO.md 업데이트해줘"

아키텍처는 `docs/architecture.md`, 초기 계획서는
`C:\Users\fredh\.claude\plans\modular-rolling-wozniak.md`.
완료 이력은 맨 아래 요약 + `git log`.

---

## 지금 할 것

**하드웨어가 1순위다.** 파이 5에 모니터·마이크·스피커·카메라·구동부가 붙어 있고,
09-01·09-02에 두 번 앞에 섰다. **다음은 세 번째다.**

### 🔴 1순위 — Gemini 무료 등급의 하루 예산 (2026-09-02 확정)

**"오늘 막혔고 내일 풀린다"가 아니다. 하루치 통의 크기가 문제다.**

> `quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`, **`limit: 20`**.
> 무료 등급은 **모델 하나당 하루 20건**이고, 통은 **모델별로 따로** 잡힌다
> (같은 시각에 `gemini-3.6-flash`는 429, `gemini-3.5-flash`는 200이었다).

| 무엇 | 쓰는 모델 | 하루 한도 |
|---|---|---|
| **대화 + 받아쓰기** | `gemini-3.6-flash` → `gemini-3.5-flash` | **합쳐서 40** |
| 음성 합성(TTS) | `gemini-3.1-flash-tts-preview` | 별도 20 (+ 디스크 캐시) |
| 표정(Vision) | 대화와 **같은 통** | 지금 꺼 둠 |

**받아쓰기가 대화와 같은 통을 쓴다** — `transcribeAudio()`도 `withRetry`를 거쳐
같은 모델 체인을 탄다. 그래서:

> ### 음성 대화 한 번 = 받아쓰기 1 + 대화 1 = **2건**
> ### 하루 40건 = **말을 20번 걸면 끝난다**

**무료로 버티기로 했다.** 그래서 2순위의 순서를 런북 순서가 아니라
**할당량을 안 쓰는 것부터**로 다시 짰다(아래).

- [x] **`VITE_VISION_ENABLED=false`** — EC2에서 껐고 재빌드·재시작까지 마쳤다.
      15초마다 돌던 Vision 호출(시간당 240건)이 사라졌다. **이걸 다시 켜면 10분 만에
      하루치가 날아간다** — 켤 일이 생기면 이 숫자를 먼저 볼 것
- [x] **할당량이 소진돼도 대체 모델로 안 넘어가던 것** — `withRetry`가 `QUOTA_EXHAUSTED`에
      `throw`로 체인 전체를 빠져나가 `GEMINI_FALLBACK_MODEL`을 시도조차 하지 않았다.
      이제 재시도만 접고 모델은 넘긴다(20 → 40). EC2 배포 후 실동작 확인
- [ ] **행사 전에는 결제를 켠다** — 시연 하나에 40건은 불가능하다(관람객이 말을 20번만
      걸어도 끝난다). 호출 빈도 조정·캐싱은 이 숫자 앞에서 의미가 없다.
      **행사 전 필수. 그때까지는 예산 안에서 움직인다**

### 🔴 2순위 — 내일 파이 앞에서 (이 순서대로)

현장 절차는 `docs/pi-runbook.md`. **단, 실행 순서는 런북이 아니라 아래를 따른다** —
런북은 §6(STT)이 맨 앞인데 그게 **제일 비싸서** 뒤가 다 굶는다.

#### 0. 앉자마자 (할당량 0건)

- [x] **EC2 커밋 확인 + 배포** — 09-03에 `813d7a0`인 것을 확인하고 VAD 관측 스위치까지 배포했다.
      **접속 직후 첫 명령이어야 한다** (09-02 오전에 2커밋 뒤처져 있던 일이 있었다)
- [x] **VAD 임계값 확정 (2026-09-03 파이 실측, 할당량 0건)** — `?vad=1` 오버레이로 쟀다.
      바닥 0.0015 / 작게 말할 때 0.06 / 평소 말투 0.15, 긴 문장도 한 발화로 남았다.
      **기본값(`startThreshold 0.02` / `endThreshold 0.012` / `silenceMs 900`)이 맞아 바꾸지 않았다.**
      근거는 `vad.js`의 주석에 적었다. 다시 재려면 런북 §6-3-A

#### A. 할당량을 전혀 안 쓰는 것 — 여기부터 끝낸다

- [x] **원격조종 왕복 지연 실측 완료 (2026-09-03)** — 09-02에 만든 것의 첫 실측이었다.
      파이↔EC2 왕복 **206~227ms**(편차 21ms), 정지 판정 **210~230ms**, 손 뗀 뒤 실제 정지
      **0.9초 안팎**. `Ctrl+C` 마지막 줄도 `stop()`이었다. 상세는 런북 §7-2.
      **재는 과정에서 결함 2건을 찾아 고쳤다**(커밋 `3901c90`):
      - 정지 판정이 처음엔 850~908ms였다 — 구동부가 "조회 실패"와 "서버가 정지라고 답함"을
        똑같이 취급해 서버 만료 위에 창이 한 겹 더 얹혀 있었다. **4배 줄었다**
      - 폰에서 화살표를 꾹 누르면 **복사·공유 메뉴**가 떴다. 메뉴가 뜨면 `pointerup`을 놓쳐
        손을 뗐는데 심박이 계속 나갈 수 있다 — 보기 문제가 아니라 안전 문제였다
- [ ] **폰↔EC2 구간 지연 측정** — 위 실측이 남긴 숙제. 남은 지연의 대부분인
      `MOVE_TTL_MS`(700ms)를 줄이려면 심박이 지나는 이 구간을 먼저 재야 한다.
      속도 상한(40)을 올릴 근거도 여기서 나온다
- [ ] **와이파이를 끊었을 때 `조회 실패 ○○ms`로 서는지** — 09-03에 시간이 없어 못 했다.
      정상 정지는 확인됐고, 이쪽이 **진짜 안전장치**다
- [x] **§9 구동부 실사 완료 (2026-09-03) — 배선 경로를 찾았다. 막힌 것은 프로토콜이다.**
      GPIO 40핀 **전부 미사용**, `/dev/i2c-1` **없음** → 직결도 모터 HAT도 아니다.
      **`1a86:7523` CH340 → `/dev/ttyUSB0` 하나가 유일한 길**(`dialout`이라 sudo 불필요).
      즉 파이가 모터를 돌리지 않고 **시리얼 건너편 MCU가 돌린다** — `motors.py`는
      GPIO 코드가 아니라 **시리얼 한 줄 쓰기**가 된다. 상세는 `motors.py` 머리말.
- [ ] 🔴 **시리얼 프로토콜을 알아내는 것 — 구동부의 유일한 남은 관문.**
      보드가 115200·9600 둘 다에서 아무것도 안 뱉는다(받기만 하는 펌웨어).
      **추측해서 바이트를 보내지 않는다** — 모르는 명령에 로봇이 예상 못 한 방향으로 움직인다.
      - [ ] **그 보드의 아두이노 스케치(.ino) 찾기** — 팀원? 다른 레포? SD카드?
            프로토콜은 거기에만 있다. 찾으면 `motors.py`는 10줄이면 끝난다
      - [ ] 못 찾으면 **펌웨어 덤프**(읽기 전용, 로봇이 움직이지 않는다):
            `avrdude -c arduino -p m328p -P /dev/ttyUSB0 -b 115200 -U flash:r:/tmp/fw.bin:r`
            → `strings -n 3 /tmp/fw.bin` 에 명령 문자열이 박혀 있을 수 있다
- [ ] **`wifi-setup.sh` 실행** — 09-01·09-02 모두 건너뛰었다. 동아리방/행사장/폰 핫스팟.
      **행사 전에는 반드시.** 행사장에서 파이가 인터넷을 못 잡으면 시연 자체가 없다
- [ ] **미디어 권한 대화상자** — 재부팅 후 또 물어보는지. 안 물어보면 프로필에 저장된
      것이라 큰 문제가 아니다. 또 물어보면 Chromium 정책 파일이 필요하다

#### B. TTS 통(별도 20건)만 쓰는 것 — 미리 캐시하면 0건

- [ ] **`npm run prewarm-tts`를 먼저 돌린다** (EC2에서) — 자주 쓰는 문장이 디스크에
      캐시되면 아래 두 항목이 할당량을 **전혀** 안 쓴다
- [ ] **말풍선 가독성** — 800×480 실물에서 읽히는지 눈으로 본다(09-02에 1rem→1.55rem).
      **`/guardian/send`로 문장을 보내면 대화 통을 안 쓴다** — 폰에서 보내고 화면만 본다
- [ ] **§8 낙상 알림 배관** — 푸시 재구독 → EC2에서 `npm run mock-detector` → 폰 푸시 →
      파이 비상 모드 → 해제. `mock-detector`는 자기 스냅샷을 실어 보내므로 Gemini를 안 쓴다.
      **원격조종 측정보다 뒤에** — 응급 상태에서는 원격조종이 `423`으로 잠긴다.
      ⚠️ **런북의 "카메라 칩 → `/guardian/live`" 첫 단계는 지금 안 된다** — Vision을 껐으므로
      스냅샷이 아예 안 올라간다("아직 찍힌 사진이 없어요"가 정상이다). 알림 배관 자체는
      그것과 무관하게 확인된다. 방 안 모습을 보려면 아래 백로그의 "Gemini 없는 스냅샷 경로"가
      필요하다 — **§8을 위해 Vision을 켜지 말 것**(10분이면 하루치가 날아간다)

#### C. 대화 통 40건을 쓰는 것 — 맨 마지막에, 남은 예산으로

- [ ] **🔴 서버측 STT 실측** (런북 §6-3) — **발화 한 번에 2건. 20번이면 끝난다.**
      - [x] **VAD 임계값이 reSpeaker에 맞는가** — 09-03 확인. 기본값 그대로 간다(위 0번)
      - [ ] 로봇이 **자기 목소리에 반응하지 않는지** (스피커 볼륨을 올려 놓고)
      - [ ] **왕복 지연 측정** — 말을 마치고 대답까지. 너무 길면 `vad.js`의 `silenceMs`부터 줄인다

### ✅ 시작하자마자 고칠 것 — 2026-09-03 완료

- [x] **VAD 판정을 밖에서 볼 수 없다** (2026-09-02 발견 → **09-03 해결**).
      `?vad=1`을 붙이면 키오스크 좌상단에 **RMS 막대 + 임계값 눈금 + 판정(idle/started/
      ended/discarded) + 직전 발화 길이**가 뜨고, **발화를 잡아도 업로드하지 않는다**(할당량 0건).
      `?vadstart=`/`?vadend=`/`?vadsilence=`로 **재빌드 없이** 값을 바꿔 볼 수 있다.
      현장 절차는 런북 §6-3-A. **확정한 값은 `frontend/src/lib/vad.js`의 `DEFAULTS`에 반영해야
      남는다** — 주소의 파라미터는 그때뿐이다. 프론트 테스트 102→112

### 🟡 3순위 — 시연 전에 결론이 필요한 것

- [ ] **TTS가 503이면 로봇이 조용히 벙어리가 된다** — 실패 시 브라우저 TTS로 폴백하는데
      **파이에서 브라우저 TTS는 무음**이다(09-01 실측). 09-02에 실제로 발생했다.
      화면에 글은 뜨므로 완전한 실패는 아니지만, 소리로만 듣는 어르신에게는 실패다.
      선택지: (a) `npm run prewarm-tts` 캐싱 — 보호자가 임의로 친 문장에는 안 통한다,
      (b) TTS에도 재시도 추가, (c) `TTS_PROVIDER=cloud` (콘솔에서 API 활성화 1회 필요)
- [ ] **파이에 물리 키보드를 상시로 둘지** — STT가 실패했을 때의 유일한 현장 대비책이다.
      파이에는 가상 키보드가 없어 화면 텍스트 입력만으로는 어르신이 쓸 수 없다.
      STT가 파이에서 통과하면 이 항목은 사라진다

### A 트랙 — 소프트웨어 (전부 로컬에서 가능)

- [ ] **팀원에게 알리기** — 우리 테이블 7개가 팀원의 `silvercare` DB에 생겼고,
      RDS 보안 그룹에 규칙 2개를 추가했다. 팀원 테이블·데이터는 그대로인 것을 확인했다
- [ ] **보호자 앱 육안 확인** — 대화 로그·알림 이력이 보이는지 → **푸시 구독 다시 등록**
      (이관된 구독은 origin이 NULL이라 정리 로직이 지운다. 의도된 동작)
- [ ] **RDS 마스터 암호 교체** — **팀원 앱 설정도 함께 바꿔야 하므로 조율이 필요**해
      제일 먼저 꺼내 둔다. 바꾼 뒤 로컬·EC2 양쪽 `.env`와 팀원 쪽을 모두 갱신하고
      `npm run verify-rds`로 확인한다. (암호 값은 이 레포에 적지 않는다.)
- [ ] **보호자 로그인(세션/JWT)** — 남은 것 중 **가장 큰 구조적 부채**. 여러 세션이 든다.
      상세는 아래 백로그
- [ ] **`purge-old-messages` 정기 실행 등록** — 삭제 범위는 확인 끝났다(아래 백로그).
      남은 건 EC2에 cron/systemd timer를 다는 일이라 로컬에서는 못 끝낸다

### B 트랙 — 하드웨어 (파이 현장 항목은 2순위로 옮겼다)

- [x] **원격조종 네트워크 지연/단절 정책** (Phase 7 잔여) — **09-02 해결.** 정지가
      "정지 신호가 도착해서"가 아니라 **"신선한 이동 의도가 끊겨서"** 일어나도록 바꿨다.
      앱이 누르고 있는 동안 250ms마다 심박을 보내고, 파이의 구동부는 신선할 때만 바퀴를
      돌린다. **폰이 꺼져도·와이파이가 끊겨도·EC2가 죽어도 로봇은 선다**(전부 로컬에서 실증).
      남은 것은 2순위의 실측으로 숫자를 확정하는 일뿐이다
- [ ] **구동부 소비자 프로세스 — 남은 것은 `motors.py` 하나뿐이다.**
      골격은 09-02에 만들었다(`deploy/pi/drivetrain/`): 200ms 폴링, 만료 판정, 조회 실패 시
      즉시 정지, 종료 신호·예외에서 `stop()`, `--dry-run`, systemd 사용자 유닛.
      **하드웨어를 아는 파일은 `motors.py` 하나**라 예전 제어 코드를 찾으면 그것만 채운다.
      지켜야 할 세 가지는 `deploy/pi/drivetrain/README.md`에 있다.
      `move`를 ack하는 것은 이 프로세스 하나뿐이어야 한다(키오스크는 08-31부터 보기만 한다)
- [ ] 파이 ↔ EC2 ↔ 보호자 앱 네트워크 구성 미정 (퍼블릭 도메인 vs 터널 유지).
      터널 유지 시 재시작마다 `set-url.sh` 한 번이 필요하다
- [ ] **YOLOv8 낙상 감지 실제 구현** — 백엔드 계약(`POST /api/detections`)과 mock은 이미
      완성돼 있어 **백엔드 변경은 불필요**하다. `detector/`에 Python(FastAPI) 서비스를 만들어
      계약대로 POST만 하면 된다. `docs/fall-detection.md` 참고.
      배포 골격은 팀원 레포의 `deploy/silvercare-api.service`(systemd)를 그대로 쓸 만하다.
      - [ ] **선행: detector용 presigned 업로드** (아래 백로그). detector가 없는 지금 미리
            만들면 쓰이지 않는 코드가 되므로 **A 트랙에 두지 않았다**
      - [ ] 카메라 → detector 데이터 흐름 미정 (RTSP / HTTP 스트리밍 / 파일 감시)
      - [ ] 파이5 CPU 추론 가속(ONNX Runtime/OpenVINO) 여부와 목표 FPS 미정

> **낙상 감지에 GPU는 필요 없다** (08-31 판단). 대회 계정에 클라우드 GPU가 없다는 것과
> 낙상 감지를 못 한다는 것은 별개다. YOLOv8n은 GPU 없이 도는 것을 전제로 만든 모델이고,
> 파이5(Cortex-A76 4코어)에서 입력을 320~416px로 낮추면 실용 FPS가 나온다 — **추론은 파이
> 로컬에서 돈다.**
>
> Gemini API로 낙상을 대체하는 것은 **구조적으로 맞지 않는다.** 낙상은 하강 속도를 가진
> *움직임*인데 Gemini에 보내는 것은 정지 프레임 한 장이라, 바닥에 누운 사람이 "넘어진 것"인지
> "누워 쉬는 것"인지 구분되지 않는다. 호출당 1~3초 지연 + 프레임당 과금도 상시 감시에 부적합하다.
> → **YOLOv8n = 순간 이벤트(고빈도) / Gemini = 표정·상태 요약(저빈도).** 지금 코드가 이미 그렇다.

---

## 프로젝트 제약 — 시작 전에 반드시 읽을 것

**대회(한이음 드림업) 제공 AWS 계정** (2026-08-27 실측 확정):

| 가능 | 불가능 |
|---|---|
| EC2, Lambda, RDS, DynamoDB, S3, API GW, Amplify, SQS, SNS | **Bedrock, Polly, Transcribe 등 클라우드 AI 전부** |

→ **AI는 Gemini API를 계속 쓴다.** 대화·비전·음성을 AWS로 옮기는 계획은 세우지 말 것.
Bedrock 어댑터를 실제로 만들어 테스트했으나 `BedrockDeny` 명시적 거부 정책이 걸려 있었고,
explicit deny는 EC2 IAM Role을 거쳐도 우회 불가라 **전부 제거했다**(2026-08-27).

**계정 공통 제약**
- **Access Key 발급 절대 불가.** 인증은 IAM Role만 — EC2는 `SafeInstanceProfile-{username}`.
  IAM Role은 AWS 안에서 도는 프로세스에만 붙으므로 **로컬에서는 인증 테스트가 불가능**하다.
  **예외: RDS**(사용자/비밀번호 인증) — 아래 "RDS 운영 메모" 참고.
- MFA 설정 후 재로그인해야 자원 생성 가능(`DenyAllWithoutMFA`).
- 지정 리전 밖에서는 모든 활동 제한 — **문제 생기면 리전부터 확인.**
- EC2는 `t3.nano`~`t3.small`만. S3 버킷 이름은 본인 username으로 시작해야 함.
- 콘솔 **CloudShell**은 로그인 세션 자격증명을 자동으로 쓰므로 Access Key 없이 CLI 사용 가능.

**남아 있는 아키텍처 함정**
- **`services/history.js`에는 변환 계층이 아예 없다.** 저장 형식이 곧 Gemini 와이어
  포맷(`{role, parts:[{text}]}`)이고 SDK의 `startChat({history})`로 **그대로** 들어간다.
  다른 LLM으로 갈아탈 일이 생기면 변환 계층을 새로 만들어야 한다. 덤으로 model 턴 인코딩이
  두 가지로 섞여 있다 — 실시간 대화는 `JSON.stringify({text,emotion})`, 재시작 복원은 평문.

---

## RDS 운영 메모

2026-08-31에 EC2까지 PostgreSQL로 전환 완료. `DB_DRIVER=sqlite`로 되돌리면 즉시
파일 DB로 복귀한다 (EC2의 `data/hyodol.sqlite`는 백업으로 남겨 뒀다).

**팀원과 같은 `silvercare` DB를 쓴다.** 우리 7개 테이블이 팀원 테이블
(`emergency_records`/`emotion_records`/`medication_records`) 옆에 있다. 지금은 이름이
겹치지 않지만 **양쪽이 테이블을 추가할 때 충돌할 수 있다.**

**네트워크 구성**(실측) — EC2·RDS·팀원 EC2가 전부 같은 기본 VPC 안에 있다. 그래서 EC2는
RDS를 **사설 IP로** 해석하고 소스도 사설 IP가 된다. 공인 IP로 인바운드를 열어도 EC2
경로에는 적용되지 않으므로 **보안 그룹 참조(source-group)로 열어야 한다.** 개발 PC는
VPC 밖이라 공인 IP(`/32`)로 열려 있고, **공인 IP가 바뀌면 다시 막힌다** — 그때는
`https://checkip.amazonaws.com`으로 새 IP를 확인해 RDS 보안 그룹에 넣는다.

**같은 함정이 EC2 SSH에도 있다** (2026-08-31 확인). 22번도 개발 PC 공인 IP `/32` 하나로만
열려 있어 **다른 와이파이로 옮기면 `npm run access`와 EC2 접속이 통째로 막힌다** — 증상은
역시 연결 거부가 아니라 **타임아웃**이다. 액세스 키 발급이 불가능한 계정이므로 콘솔
**CloudShell**에서 `authorize-security-group-ingress`로 새 IP를 넣거나, **EC2 Instance
Connect**(AWS 대역이라 어디서든 열린다)로 우회한다. 절차는 `docs/pi-runbook.md` §0-A.

**로컬 `.env`도 pg를 본다.** 로컬에서 본 것이 곧 보호자 앱에 보이는 것이라 편하지만,
로컬 대화 테스트 데이터가 실제 RDS에 쌓인다는 뜻이기도 하다.

**테스트는 `.env`와 무관하게 항상 SQLite로 돈다** — 각 테스트 파일이 `DB_DRIVER='sqlite'`를
직접 고정한다. 이 핀을 빼면 `retention.test.js`가 실제 RDS의 메시지를 지운다.

**⚠️ `verify-rds`를 반드시 돌려야 하는 이유** — `pg-driver.test.js`는 pg-mem으로 도는데,
**변이 테스트로 확인한 결과** 아래 둘은 코드를 망가뜨려도 통과해 버린다:

| | 왜 안 잡히나 |
|---|---|
| 트랜잭션 **롤백** | pg-mem의 `pool.connect()`가 격리 세션이 아니라 BEGIN/ROLLBACK이 no-op |
| **COUNT/id 타입** | 진짜 node-pg는 int8(COUNT·BIGINT)을 **문자열**로 주는데 pg-mem은 늘 숫자 |

둘 다 실제 RDS에서 통과하는 것을 확인했다(로컬·EC2 양쪽). 드라이버를 건드리면 다시 돌릴 것.

## 운영 메모

**EC2의 `TTS_PROVIDER=gemini`** (2026-09-01에 추가). 파이에서 브라우저 TTS가 무음이라
서버측으로 돌렸다. 그 줄은 원래 `.env`에 **아예 없었다**(기본값 `browser`로 동작 중이었다).
EC2를 새로 만들거나 `.env`를 갈아엎으면 **다시 넣어야 로봇이 말을 한다.**

**실측 지연** (quick tunnel 경유):
- 폰 `/guardian/send` → 파이 발화: **5초 안팎** (09-01). 구간은 키오스크 폴링 대기(평균
  1.25초) + Gemini 음성 생성(1~3초) + 터널 전송. `npm run prewarm-tts`로 캐싱하면 자주
  쓰는 문장은 생성 구간이 사라지지만, 보호자가 임의로 친 문장은 그대로다.
- **받아쓰기(`POST /api/stt`)**: 같은 3.6초 오디오를 4번 보내 **3.0 / 3.4 / 20.3 / 52.3초**
  (09-02, EC2에서 측정). **잘 되면 3초, Gemini가 붐비면 자릿수가 달라진다.** 느린 쪽은
  전부 503 재시도 체인이었고, 그래서 시한(`STT_TIMEOUT_MS`, 기본 12초)을 넣었다.
  받아쓰기 정확도 자체는 문제없었다 — "아이고 허리야 못 일어나겠어"를 "아이코 허리야
  못 일어나겠어"로 옮겼다.
- 원격조종 왕복은 아직 측정 못 했다 — B 트랙의 데드맨 정책이 이 숫자를 기다리고 있다.

**서버측 STT가 감수하는 것 두 가지** (2026-09-02부터. 선택이 아니라 사실로 적어 둔다):
- **방 안의 모든 발화가 Gemini로 올라간다.** 웨이크워드 판정이 받아쓰기 *뒤에* 일어나므로
  게이트가 닫혀 있어도 호출은 발생한다. 로컬 웨이크워드 감지 없이는 구조적으로 못 피한다.
  지금은 에너지 임계값 + 최소 0.4초 + 최대 12초로만 거른다. **할당량을 쓰는 주범이다.**
- **지연이 늘어난다.** 브라우저 STT는 말하는 도중에 인식했지만, 이제는 **말이 끝나야**
  시작한다(침묵 0.9초 감지 → 업로드 → 받아쓰기).

**파이 설정 실측값** (2026-09-02, `~/.config/hyodol/kiosk.env`):
- `KIOSK_ROTATE=180` — 이 기기의 7인치 화면은 180도 회전이 맞다. **터치 좌표도 함께
  돌았다**(확인 완료 — 화면만 돌고 터치는 안 도는 흔한 함정을 피했다).
- 기본 출력 `UACDemoV1.0`(USB 스피커) / 기본 입력 `reSpeaker 4-Mic Array` — 둘 다 정상.
- **파이에서는 `git config core.fileMode false`를 걸어 뒀다.** 안 그러면 `chmod +x` 한 것이
  로컬 수정으로 남아 다음 `git pull`을 막는다.

**인스턴스를 껐다 켤 때마다** — 새 퍼블릭 IP로 한 줄만 돌리면 `ACCESS.html`(gitignore)에
파이용/폰용 링크와 QR이 갱신된다. `reboot`은 IP가 유지되지만 **stop/start는 바뀐다.**

```bash
npm run access -- <새 퍼블릭 IP>     # 끌 때는: npm run access -- --stopped
```

**배포된 앱의 알려진 한계 3가지** — 전부 `docs/deploy-ec2-production.md`의 "알려진 한계"에
상세가 있다. 요약만:
1. **quick tunnel은 SSE를 통과시키지 못한다**(실측, 코드로 못 고침). 응급 푸시는 FCM
   직통이라 무사하고, 보호자 앱은 30초 폴백 폴링으로 동작한다.
2. **터널 주소가 재시작마다 바뀐다** → 브라우저 기준 다른 사이트라 **푸시 구독을 다시 해야
   한다.** 고정 주소 + 실시간이 동시에 필요해지면 도메인 + Let's Encrypt 직결.
3. `ROBOT_API_KEY`는 진짜 인증이 아니다 → 백로그의 "보호자 로그인" 항목.

**인스턴스 크기**: `t3.small`(1.9Gi)면 충분하다(실측 — `npm install` ×2 + 프론트 빌드 +
백엔드 상시 기동에 여유 1.1Gi 이상). `t3.nano`(512MB)는 `@aws-sdk/client-s3` 설치에서
OOM 위험이 있어 권하지 않는다. 24시간 가동 시 월 $15 안팎.

**로컬에서 HTTPS로 폰 테스트할 때** — `npm run dev`가 아니라 **preview를 터널링**해야 한다.
서비스 워커는 `main.jsx`의 `import.meta.env.PROD` 가드 때문에 프로덕션 빌드에서만 등록된다.

```bash
cd frontend && npm run build && npm run preview        # 4173
C:\Users\fredh\bin\cloudflared-windows-amd64.exe tunnel --url http://localhost:4173
```

preview가 `/api`를 3001로 프록시하므로 터널은 하나면 된다.

---

## 백로그

로드맵에 안 들어갔지만 언젠가 해야 할 것들. 발견 시점과 이유를 함께 적는다.

- [ ] **Gemini 없는 스냅샷 경로** (2026-09-02, Vision을 끄면서 생김). 보호자 앱의
      "방 안 모습"(`/guardian/live`)이 `/api/vision`에 실려 오는 사진에만 의존한다.
      그런데 그 경로는 **사진을 저장하기 전에 Gemini 분석을 반드시 거친다**
      (`routes/vision.js:30`에서 `latestSnapshot`을 먼저 넣긴 하지만, 애초에 프론트가
      Vision이 꺼져 있으면 호출 자체를 안 한다). 그래서 할당량을 아끼려고 Vision을 끄면
      **보호자가 집 안을 볼 수단이 통째로 사라진다.**
      → 분석 없이 사진만 올리는 경로가 있으면 둘을 분리할 수 있다(표정 요약은 저빈도로,
      사진은 상시로). 원격조종을 화면 보면서 하려면 어차피 필요하다
- [ ] **키오스크의 `⬅️ 이동 중` 표시가 이동 시간을 못 따라간다** (2026-09-02, 꾹 누르기로
      바꾸면서 생김). 큐에 방향 전환마다 한 줄만 넣으므로 5초를 계속 밀어도 표시는
      1.5초 뒤에 사라진다. **로봇이 어르신 앞에서 움직이는데 화면은 조용한 상태**가 되므로,
      실제로 바퀴가 도는 순간부터는 고쳐야 한다. 키오스크가 `GET /api/control/state`를
      함께 보고 `moving`인 동안 표시를 유지하는 쪽이 맞다. **`motors.py`를 채울 때 같이 한다** —
      지금은 모터가 안 돌아 표시할 움직임 자체가 없다.
- [x] **미처리 `move` 명령이 쌓인다** (2026-08-31 발견 → **09-02 크게 완화**).
      꾹 누르기로 바꾸면서 이동 의도가 큐를 떠났다 — 이제 큐에는 **방향이 바뀔 때 한 줄만**
      남는다(심박마다 넣었다면 초당 네 줄이었을 것이다). 여전히 구동부가 ack하기 전까지는
      남지만 양이 한 자릿수로 줄었다. `motors.py`가 채워지면 완전히 해소된다.
- [ ] **미디어 권한 대화상자가 뜬다** (2026-09-01 실측). `kiosk.sh`의
      `--use-fake-ui-for-media-stream`이 항상 먹지는 않아서, 파이 화면에 카메라 권한
      대화상자가 떴다. 오늘은 마우스로 눌렀지만 **어르신 집·행사장에는 누를 사람이 없다** —
      그러면 카메라와 마이크가 통째로 죽는다. 먼저 **프로필에 권한이 영구 저장되는지**
      확인하고(그러면 첫 설치 때 한 번만 누르면 된다), 아니면 Chromium 정책 파일의
      `VideoCaptureAllowedUrls`/`AudioCaptureAllowedUrls`에 터널 주소를 등록한다.
- [ ] **detector용 presigned 업로드** (2026-08-30). YOLOv8 detector가 낙상 스냅샷을 보낼 때
      지금 계약(`POST /api/detections`의 base64 data URI)은 12MB JSON 바디를 Express로
      통과시킨다. 팀원 `app/s3_service.py`의 presigned PUT 방식이 이 경우엔 더 낫다
      (`incoming/{category}/{YYYY}/{MM}/{DD}/{uuid}.{ext}` 키 규칙까지 쓸 만하다).
      **보호자 앱 경로에는 쓰지 말 것** — `/api/snapshots/:filename` 프록시 서빙은 LAN 키
      인증이 안 깨지게 일부러 그렇게 만든 것이다.
- [ ] **보호자 로그인(제대로 된 인증)** — 2026-08-29 EC2 상시 배포로 **위험도가 올라갔다.**
      `ROBOT_API_KEY`는 클라이언트 번들에 들어가므로 진짜 인증이 아니다. 시연·발표용으로
      수용한 상태이며 실사용 전에는 세션/JWT 기반 로그인이 필요하다.
- [ ] **팀원과 테이블 이름이 충돌할 위험** (2026-08-31, RDS 전환하며 생김). 같은
      `silvercare` DB를 쓰게 됐다. 지금은 안 겹치지만(우리 `medications` vs 팀원
      `medication_records`) 양쪽이 테이블을 추가하면 언제든 겹친다. `initDB()`가
      `CREATE TABLE IF NOT EXISTS`를 돌리므로 **먼저 만든 쪽 스키마가 조용히 이긴다.**
      PostgreSQL 스키마 분리(`hyodol.` 접두)를 검토할 것.
- [ ] **EC2의 `data/hyodol.sqlite` 정리** (2026-08-31). RDS 전환 후 백업으로 남겨 뒀다.
      한동안 문제없이 도는 것을 확인하면 지운다. 지금 지우면 되돌릴 곳이 없어진다.
- [ ] `purge-old-messages` 정기 실행 등록 (지금은 수동, 스케줄 없음).
      삭제 범위는 확인했다(2026-08-31) — `repositories/messages.js`의
      `DELETE FROM messages` 하나뿐이라 **팀원 테이블에 닿지 않는다.**
- [ ] 목소리 품질을 급히 올려야 하면 Cloud TTS 전환 — `TTS_PROVIDER=cloud` +
      `npm run prewarm-tts`. 콘솔에서 API 활성화 1회 필요.
      (Polly는 계정에서 불가능하므로 이게 유일한 업그레이드 경로다)

**하지 말 것**
- ⛔ **팀원 `requirements.txt`를 그대로 설치하지 말 것** (2026-08-30). 오염된 venv의
  `pip freeze`라 FastAPI와 무관한 `agent-detector==1.1.0`, `detect-installer==0.1.0`,
  `fastar==0.12.0`이 섞여 있다. `detector/` venv를 만들 때도 재사용하지 말고
  fastapi/uvicorn/ultralytics만 직접 명시할 것.

---

## 완료 요약

상세는 `git log`와 각 문서에 있다. 여기엔 **날짜와 한 줄**만 남긴다.

| 날짜 | 항목 | 상세 위치 |
|---|---|---|
| 09-02 | **원격조종을 꾹 누르는 방식으로** — 정지가 "신호가 도착해서"가 아니라 **"신선한 이동 의도가 끊겨서"** 일어나게 바꿨다. 폰이 꺼져도·와이파이가 끊겨도·EC2가 죽어도 로봇은 선다. 심박은 큐에 쌓지 않고 방향 전환 때만 한 줄(백로그 "미처리 move" 완화). **파이 구동부 프로세스 골격**(`deploy/pi/drivetrain/`) — `motors.py`만 남았다. 문서: `architecture.md`의 낡은 명령 큐 절을 고치고 원격조종 절 신설. 테스트 백엔드 123→132 / 프론트 96→102 | `deploy/pi/drivetrain/README.md` |
| 09-02 | **Gemini 무료 등급의 정체 파악** — "일시적 소진"이 아니라 **모델당 하루 20건**. 대체 모델로 안 넘어가던 결함을 고쳐 20→40. EC2의 Vision을 껐다(시간당 240건). 결론: **시연에는 결제가 필요하다** | 위 1순위 |
| 09-02 | **서버측 STT** — 파이가 마이크를 직접 녹음해 `POST /api/stt`로 보내고 Gemini가 받아쓴다(Chromium STT 실패를 우회). 화면 회전 고정(`KIOSK_ROTATE=180` 확정, 터치 좌표 함께 회전) + 말풍선 확대·레이아웃 넘침 해결. **파이 실측에서 결함 6건**을 더 찾아 고쳤다 — 받아쓰기 실패가 침묵처럼 보이던 것, 50초 재시도, 포기 후에도 도는 마이크, 할당량 소진 재시도, 사전점검 오탐, 런북 브랜치 오류. **STT 실동작은 Gemini 할당량 소진으로 확인 못 함.** 테스트 백엔드 100→123 / 프론트 53→96 | `docs/pi-runbook.md` §3-B·§6-3 |
| 09-01 | **파이 5 실물 연동 §0~§7** — 자동실행·카메라·마이크 기본장치 통과, 서버측 TTS로 발화 해결, STT 실패 확정. 결함 7건을 찾아 스크립트·문서·코드에 반영. 프론트 52→53 | `docs/pi-runbook.md` |
| 08-31 | **파이 연동 사전 준비** — 키오스크가 실패를 화면에 드러내도록 하드닝(보안 컨텍스트·STT 오류 분류·발화 워치독·SW 경로), `move` ack 충돌 해소, `deploy/pi/` 자산. 테스트 백엔드 99→100 / 프론트 43→52 | `docs/deploy-raspberry-pi.md` |
| 08-31 | **RDS 실연결 + EC2 전환 완료** — 보안 그룹(VPC 내부는 source-group), `silvercare` DB로 108건 이관, EC2 배포까지. `verify-rds` 6개 항목 로컬·EC2 양쪽 통과 | 위 "RDS 운영 메모" |
| 08-31 | **감정 이력** — 표정이 바뀔 때만 `detections`에 기록. 보호자 홈이 로봇 발화가 아니라 어르신 표정을 근거로 말한다. 테스트 백엔드 96→99 / 프론트 39→43 | `git log` |
| 08-31 | **백로그 4건** — 스냅샷 `Content-Type`, 감지 응답의 `suppressedBy`(쿨다운/임계값 구분), 푸시 구독 origin 정리, 보호자 앱 오프라인 안내 기준 변경 | `fix/backlog` 브랜치 |
| 08-31 | **테스트 보강** — `useGuardianData`(SSE 정체/폴백), `RobotFaceDisplay`(웨이크워드 게이트 배선). 백엔드 95→96 / 프론트 29→39 | `frontend/CLAUDE.md` |
| 08-31 | 테스트가 `.env`의 `DB_DRIVER`를 따라가 실제 RDS를 지울 수 있던 결함 수정 / 이관 스크립트가 옛 SQLite에서 실패하던 문제 수정 | 아래 "살아남은 교훈" |
| 08-30 | **RDS PostgreSQL 이전** — `DB_DRIVER=sqlite\|pg` 드라이버, `schema.pg.sql`, 이관/검증 스크립트, `emergency` 트랜잭션화. 테스트 75→95 | `backend/CLAUDE.md`, 위 "RDS 운영 메모" |
| 08-30 | **복약 관리** — 팀원 코드에서 복약만 흡수. 로봇 알림 + 음성 복용 확인. 테스트 백엔드 54→75 / 프론트 22→29 | 아래 "살아남은 교훈" |
| 08-29 | 접속 주소 파일(`ACCESS.html`) + `npm run access` 갱신 스크립트 | 위 운영 메모 |
| 08-29 | 푸시 구독 재등록 버그 수정 (`ensurePushRegistered()`) | 아래 "살아남은 교훈" |
| 08-29 | **EC2 상시 배포** — `PUBLIC_DIR` 같은 오리진 서빙 + cloudflared + systemd | `docs/deploy-ec2-production.md` |
| 08-29 | **S3 스냅샷 실연결 검증** — 버킷 `project9-80-oregon-hyodol-snapshots`(`us-west-2`) | `docs/deploy-ec2-aws-test.md` |
| 08-29 | 프론트엔드 테스트 환경(Vitest + jsdom/RTL) 도입 | `frontend/CLAUDE.md` |
| 08-29 | 리포지토리 전면 async 전환 (RDS 선행 작업) + `/review` 발견 5건 수정 | `git log` |
| 08-29 | INVESTIGATE 2건 — SSE 정체 감지(named heartbeat), 스냅샷 provider 접두어 | `git log` |
| 08-28 | 알림 상세 화면 + 푸시 딥링크 / `/ship` 커버리지 감사 + 안정성 수정 | `git log` |
| 08-27 | Phase 5(응급 푸시), Phase 7(원격조종), 코드리뷰 5건, 정리 라운드 | `git log` |
| 08-27 | Bedrock 어댑터 구현 → 계정 미지원 확인 → 전면 제거 | 위 "프로젝트 제약" |
| 08-26 | Phase 0~4 (기반 재설계·대화 안정화·응급 감지·대화 로그·보호자 PWA) | 계획서 |

### 살아남은 교훈 — 다시 겪지 않기 위해

- **호출 한 번이 요청 한 건이 아니다** (09-02). "하루 20건"을 보고 넉넉하다고 생각했는데,
  **말 한 번이 받아쓰기 1 + 대화 1이라 2건**이었다. 게다가 받아쓰기가 대화와 **같은 통**을
  쓴다는 것은 `transcribeAudio()`가 `withRetry`를 거친다는 사실을 읽고 나서야 알았다.
  한도를 볼 때는 숫자가 아니라 **어느 통을 몇 번 쓰는지**까지 셀 것.
- **조정하려면 볼 수 있어야 한다** (09-02). VAD 임계값을 맞추는 일이 "발화를 반복하는
  일"이 돼 있는데, 반복 한 번마다 예산이 깎인다. **튜닝해야 하는 값에는 공짜로 관측하는
  수단이 같이 있어야 한다** — 없으면 조정 자체가 자원을 태운다.
- **정지는 신호가 아니라 침묵으로 만들어야 한다** (09-02). 원격조종을 꾹 누르기로 바꾸며
  처음엔 "떼면 정지 신호를 보낸다"로 설계했는데, 그러면 **신호가 못 가는 모든 경우**
  (폰 잠금·와이파이 끊김·서버 다운)에 로봇이 계속 간다. 안전장치는 *무언가 도착해야*
  작동하는 형태면 안 된다 — *아무것도 안 오면* 작동하는 형태여야 한다.

- **인터페이스가 같다고 생애주기까지 같지는 않다** (09-02). 서버 STT를 만들며
  `createRecognizer` 계약(`start`/`stop`/`onResult`…)을 그대로 지켰고, 그래서 호출부가
  안 바뀐다고 자신했다. 실제로는 **브라우저 STT는 오류가 나면 세션이 스스로 끝나는데
  서버 STT는 끝나지 않았다.** 포기 처리가 인식기를 멈추지 않아, 화면에는 "글로 말씀해
  주세요"를 띄워 놓고 뒤에서는 **한 시간에 100건**을 계속 올려 API 할당량을 태웠다.
  계약을 맞출 때 메서드 이름만 보고 **"언제 끝나는가"** 를 안 본 탓이다.
- **같은 상태 코드가 다른 뜻일 수 있다** (09-02). 429는 분당 한도(잠시 후 풀린다)와
  할당량 소진(오늘은 안 풀린다) 둘 다이다. 뭉뚱그려 "일시적"으로 두면 재시도가
  **남은 할당량을 더 태운다.** 숫자가 아니라 오류 문구로 갈라야 했다.
- **점검 스크립트가 틀리면 점검이 시간을 잡아먹는다** (09-02). `preflight.sh`가 멀쩡한
  스피커를 "기본 출력이 마이크입니다"로 오탐했는데(구획을 못 끊어 마이크의 기본 장치까지
  집었다), 하필 **09-01의 진짜 문제와 문구가 같아서** 없는 문제를 쫓게 만들었다.
  경고를 믿기 전에 그 경고를 만든 코드를 볼 것.
- **테스트가 기본 경로를 안 지나가고 있었다** (09-02). `RobotFaceDisplay.test.jsx`는
  브라우저 STT 이벤트를 손으로 흘려보내려고 `VITE_STT_MODE=browser`로 고정해 도는데,
  **실제 배포 기본값은 `server`다.** 그대로 뒀으면 파이가 쓰는 경로를 아무 테스트도
  안 지나갔을 것이다. 모드 스위치를 만들 때는 **어느 모드가 테스트되고 있는지** 셀 것.
- **"안 불렸다"는 그냥 기다려서 증명되지 않는다** (09-02). 빈 받아쓰기를 무시하는지
  확인하는 테스트가, 콜백이 **아직 안 온 것**과 **영영 안 오는 것**을 구분 못 해서
  가드를 지워도 통과했다. 뒤에 진짜 발화를 하나 더 보내 **"그것 하나만" 받았는지**로
  바꾸고 나서야 잡혔다.
- **길이가 같다고 같은 것이 아니다** (09-02). 발화가 중간에 끊겨 **둘로 쪼개졌는데**
  마지막 조각의 길이가 우연히 정상과 같아 테스트가 통과했다. 개수까지 함께 볼 것.
- **레이아웃은 재 보기 전까지 맞는지 모른다** (09-02). 800×480에서 얼굴이 **이미 자리보다
  컸다**(212px 자리에 264px). 말풍선을 키우자 그제서야 겹침이 눈에 보였을 뿐, 넘침 자체는
  전부터 있었다. 브라우저로 실제 크기에서 열어 `scrollHeight`를 재고 나서야 알았다.
- **셸에 백틱을 넘기지 말 것** (09-02). 한국어 문서를 `node -e "..."` 로 고치다가 백틱
  안의 코드 표기가 셸에 먹혀 문장이 **조용히 빈칸으로** 저장됐다. 파일에 먼저 쓰고
  스크립트로 읽어 넣을 것. **CRLF도 같은 함정** — 앵커 문자열이 안 맞으면 조용히 실패한다.
- **증상과 원인이 같은 층에 있다고 가정하지 말 것** (09-01). 자동실행이 두 곳에 걸린 사고의
  문서상 증상은 "Chromium이 두 개 뜸"이었지만, 실제로는 **화면이 3초 주기로 깜빡였고
  Chromium 프로세스는 하나였다.** 두 번째 `kiosk.sh`가 띄운 Chromium이 첫 번째에 URL만
  넘기고 즉시 종료했기 때문이다. 세야 할 것은 브라우저가 아니라 **감시 루프**였다.
  문서에 증상을 적을 때는 **눈에 보이는 그대로** 적을 것.
- **문서의 명령은 실제로 돌려 보기 전까지 틀린 것이다** (09-01). 런북이 시킨
  `sed '/hyodol/d'`는 **아무것도 지우지 않았다** — 그 줄에 `hyodol`이라는 문자열이 없었다
  (경로가 `silver-care-robot`이다). 절차서에 명령을 쓸 때는 그 명령이 다룰 **실제 문자열을
  눈으로 확인**할 것.
- **하드웨어는 서로의 기본값을 빼앗는다** (09-01). USB 마이크 어레이를 꽂자 **기본 출력**까지
  그쪽으로 잡혀 소리가 통째로 죽었다. `speaker-test`는 `-524`만 뱉어 원인을 못 알려준다.
  점검 스크립트는 "장치가 있는가"가 아니라 **"어느 것이 기본인가"** 를 보여줘야 한다.
- **폴백 경로도 실물에서 확인할 것** (09-01). STT 실패에 대비해 텍스트 입력을 만들어 뒀는데,
  정작 파이에는 **가상 키보드가 없어** 그 폴백을 쓸 수 없었다. 대비책을 세울 때 그 대비책이
  요구하는 것까지 따라갈 것.
- **"어댑터만 갈면 된다"는 세 번 중 한 번만 맞았다.** S3는 정말 `.env` 세 줄로 끝났지만,
  Bedrock은 계정의 explicit deny에 막혔고(스키마 CHECK 제약까지는 어댑터가 못 덮는다),
  pg는 리포지토리 40곳 + 트랜잭션 구조 변경이었다. 추정 전에 호출 지점부터 세어 볼 것.
- **로컬에서 절대 못 찾는 버그가 있다.** 푸시 구독 재등록 버그는 EC2에서 DB가 새로
  시작해야만 재현됐다(폰은 "허용됨"인데 서버엔 구독 0건 → 배너가 안 떠서 재등록 경로가
  없음). 배포 환경에서만 드러나는 상태 차이를 의심할 것.
- **테스트는 일부러 깨뜨려 보고 믿는다.** 이 프로젝트의 관례다. 실제로 복약·드라이버
  작업에서 "통과하지만 아무것도 증명하지 않는" 단언을 이 방법으로 두 번 찾아냈다
  (pg-mem이 COUNT/id 타입을 정규화해 버리는 문제 — 위 "RDS 운영 메모"의 표 참고).
- **알림 경로는 하나여야 한다.** 팀원 `emergency_records`를 합치지 않은 이유이고,
  복약 미복용도 `raise()`를 거치게 한 이유다. `warning`으로만 올리는 것도 같은 맥락 —
  약 한 번 걸렀다고 푸시를 보내면 보호자가 알림을 꺼버린다(CLAUDE.md 규칙 5).
- **계획 때 배제한 이유를 새 후보에도 적용할 것.** API Gateway를 "SSE가 깨져서" 배제해
  놓고 cloudflared는 확인하지 않아 같은 함정에 빠졌다.
- **환경 설정이 테스트로 샌다.** `.env`에 `DB_DRIVER=pg`를 넣는 순간 통합 테스트가 실제
  RDS를 치게 돼 있었다 — `retention.test.js`는 메시지를 **삭제**한다. 테스트가 무엇을
  덮어쓰는지(`DB_PATH`)만 보고 무엇을 **안 덮어쓰는지**(`DB_DRIVER`)는 안 본 탓이다.
  격리는 "덮어쓴 목록"이 아니라 "설정이 흘러들어올 수 있는 경로" 기준으로 확인할 것.
- **배포 환경의 코드 버전부터 확인할 것.** EC2가 `main`보다 14커밋 뒤에 있어서 "`.env`
  두 줄 추가"라고 계획했던 일이 실제로는 pull + 의존성 설치 + 프론트 재빌드였다.
  설정 변경이라고 가정하기 전에 거기서 도는 커밋을 먼저 볼 것.
  **또 걸렸다 (09-02).** Vision을 끄러 들어가 보니 EC2가 2커밋 뒤였고, 하필 빠진 것이
  "포기한 뒤에도 마이크가 계속 돌며 할당량을 태우던 것"의 수정이었다. **할당량을 아끼려는
  작업을 하는 동안 배포본은 할당량을 태우고 있었다.** 커밋 확인은 배포 전이 아니라
  **접속 직후 첫 명령**이어야 한다.
- **같은 VPC 안에서는 공인 IP 규칙이 소용없다.** EC2가 RDS를 사설 IP로 해석하므로 소스도
  사설 IP가 된다. 증상이 연결 거부가 아니라 **타임아웃**이라 원인을 찾는 데 시간이 걸렸다.
