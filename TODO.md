# TODO

효돌이 프로젝트 작업 목록. Claude Code가 이 파일을 읽고 다음 작업을 이어간다.

**사용법** — 작업 시작: "TODO.md 읽고 첫 번째 항목부터 시작해" / 세션 종료: "TODO.md 업데이트해줘"

- 아키텍처: `docs/architecture.md`
- 현장 절차: `docs/pi-runbook.md`
- **다시 겪지 않으려고 적어 둔 교훈 28개: `docs/lessons.md`** — 새 작업 전에 훑을 것
- 완료 이력: 맨 아래 요약 + `git log`

---

## 지금 할 것

**하드웨어가 1순위다.** 파이 5에 모니터·마이크·스피커·카메라·구동부가 붙어 있고,
09-01·09-02·09-03에 세 번 앞에 섰다. **다음은 네 번째다.**

### 🔴 1순위 — Gemini 무료 등급의 하루 예산 (2026-09-02 확정)

**"오늘 막혔고 내일 풀린다"가 아니다. 하루치 통의 크기가 문제다.**
`quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`, **`limit: 20`** —
무료 등급은 **모델 하나당 하루 20건**이고 통은 **모델별로 따로** 잡힌다.

| 무엇 | 쓰는 모델 | 하루 한도 |
|---|---|---|
| **대화 + 받아쓰기** | `gemini-3.6-flash` → `gemini-3.5-flash` | **합쳐서 40** |
| 음성 합성(TTS) | `gemini-3.1-flash-tts-preview` | 별도 20 (+ 디스크 캐시) |
| 표정(Vision) | 대화와 **같은 통** | 지금 꺼 둠 |

**받아쓰기가 대화와 같은 통을 쓴다** (`transcribeAudio()`도 같은 모델 체인). 그래서:

> ### 음성 대화 한 번 = 받아쓰기 1 + 대화 1 = **2건**
> ### 하루 40건 = **말을 20번 걸면 끝난다**

무료로 버티기로 했다. 그래서 2순위는 런북 순서가 아니라 **할당량을 안 쓰는 것부터**다.

- [ ] **행사 전에는 결제를 켠다** — 시연 하나에 40건은 불가능하다(관람객이 20번만 말을 걸어도
      끝난다). 호출 빈도 조정·캐싱은 이 숫자 앞에서 의미가 없다. **행사 전 필수.**
- ⚠️ **`VITE_VISION_ENABLED`를 다시 켜면 10분 만에 하루치가 날아간다**(15초마다 = 시간당 240건).
      켤 일이 생기면 이 숫자를 먼저 볼 것.

### 🔴 2순위 — 파이 앞에서 (이 순서대로)

절차는 `docs/pi-runbook.md`. **단, 실행 순서는 런북이 아니라 아래를 따른다** — 런북은
§6(STT)이 맨 앞인데 그게 **제일 비싸서** 뒤가 다 굶는다.

#### 0. 앉자마자 (할당량 0건)

- **EC2 커밋 확인이 접속 직후 첫 명령이어야 한다.** 09-02·09-03 두 번 다 뒤처져 있었다.

#### A. 할당량을 전혀 안 쓰는 것 — 여기부터 끝낸다

- [ ] **폰↔EC2 구간 지연 측정** — 09-03 실측이 남긴 숙제. 남은 지연의 대부분인
      `MOVE_TTL_MS`(700ms)를 줄이려면 심박이 지나는 이 구간을 먼저 재야 한다.
      속도 상한(40)을 올릴 근거도 여기서 나온다
- [ ] **와이파이를 끊었을 때 `조회 실패 ○○ms`로 서는지** — 09-03에 시간이 없어 못 했다.
      정상 정지는 확인됐고, 이쪽이 **진짜 안전장치**다
- [ ] 🔴 **펌웨어가 반복 명령을 못 받는다 — 연속 주행의 걸림돌.**
      한 번 보내면 잠깐 돌고, 0.2·0.5·1초 간격으로 반복하면 아예 안 돈다(09-03 실측).
      그래서 지금은 **의도가 바뀔 때 한 글자만** 보낸다. 따라오는 한계 넷:
      연속 주행 불가능할 수 있음 / 걸렸을 때 자동 복구 없음(손을 뗐다 다시 누르면 복구) /
      **속도 조절 불가**(`MAX_SPEED=40`이 아무것도 제한하지 않는다) / `S` 유실 대비 불가
      - [ ] **팀원에게 `.ino` 원본 받기** ← 가장 빠른 해결. 추측이 전부 사실 확인으로 바뀐다
      - [ ] 못 구하면 **멱등 + 자체 타임아웃 펌웨어를 새로 올린다.** 원본 백업이 파이의
            `~/firmware-backup/`에 있어 되돌릴 수 있다. 모터 핀 배선 파악이 선행
      - [ ] **모터 전원이 AA 배터리팩이다** — 시연 중 전압이 떨어지면 소프트웨어 결함과
            구분되지 않는다. 행사 전에 충전식/어댑터를 검토할 것
- [ ] **`wifi-setup.sh` 실행** — 09-01·09-02·09-03 모두 건너뛰었다. 동아리방/행사장/폰 핫스팟.
      **행사 전 필수.** 행사장에서 파이가 인터넷을 못 잡으면 시연 자체가 없다
- [ ] **미디어 권한 대화상자가 재부팅 후 또 뜨는지** — 09-01에 파이 화면에 떴고 마우스로 눌렀다.
      **어르신 집·행사장에는 누를 사람이 없다** — 그러면 카메라와 마이크가 통째로 죽는다.
      프로필에 영구 저장되면 첫 설치 때 한 번으로 끝난다. 아니면 Chromium 정책 파일의
      `VideoCaptureAllowedUrls`/`AudioCaptureAllowedUrls`에 터널 주소를 등록한다

#### B. TTS 통(별도 20건)만 쓰는 것 — 캐시된 문구는 0건

- [ ] **말풍선 가독성** — **720×1280 세로** 실물에서 읽히는지 눈으로 본다.
      **`/guardian/send`로 문장을 보내면 대화 통을 안 쓴다** — 폰에서 보내고 화면만 본다.
      자주 쓰는 문구 9개는 캐시돼 있어 **TTS 통도 안 쓴다**(09-03 예열 완료)
- [ ] **§8 낙상 알림 배관** — 푸시 재구독 → EC2에서 `npm run mock-detector` → 폰 푸시 →
      파이 비상 모드 → 해제. `mock-detector`는 자기 스냅샷을 실어 보내 Gemini를 안 쓴다.
      **원격조종 측정보다 뒤에** — 응급 상태에서는 원격조종이 `423`으로 잠긴다.
      ⚠️ 런북의 "카메라 칩 → `/guardian/live`" 첫 단계는 **지금 안 된다**(Vision을 꺼서
      스냅샷이 안 올라간다 — "아직 찍힌 사진이 없어요"가 정상). 알림 배관은 그와 무관하게
      확인된다. **§8을 위해 Vision을 켜지 말 것.** 방 안을 보려면 백로그의 "Gemini 없는
      스냅샷 경로"가 필요하다

#### C. 대화 통 40건을 쓰는 것 — 맨 마지막에, 남은 예산으로

- [ ] **🔴 서버측 STT 실측** (런북 §6-3) — **발화 한 번에 2건. 20번이면 끝난다.**
      VAD 임계값은 09-03에 기본값이 맞다고 확정했다(할당량 0건으로 측정)
      - [ ] 로봇이 **자기 목소리에 반응하지 않는지** (스피커 볼륨을 올려 놓고)
      - [ ] **왕복 지연 측정** — 말을 마치고 대답까지. 길면 `vad.js`의 `silenceMs`부터 줄인다

### 🟡 3순위 — 시연 전에 결론이 필요한 것

- [ ] **무음 표시가 파이에서 뜨는지가 진짜 검증이다** (09-03 밤에 만들었다). 서버 TTS가
      실패해 브라우저 TTS로 넘어갔는데 소리가 안 나면 말풍선에
      `🔇 소리가 나지 않아 글로만 전해 드렸어요`가 뜬다. **뜨면** 브라우저 TTS 폴백이
      파이에서 쓸모없다는 것이 화면으로 확정되고, 그때 아래 cloud 전환이 필수가 된다
- [ ] **`TTS_PROVIDER=cloud` 전환 판단** — 콘솔에서 API 활성화 1회 필요. 재시도가 provider와
      무관하게 동작하므로 활성화하는 순간 `.env` 한 줄로 바뀐다. 목소리 품질도 같이 올라간다
      (Polly는 계정에서 불가능하므로 이게 유일한 업그레이드 경로다)
- [ ] **파이에 물리 키보드를 상시로 둘지** — STT가 실패했을 때의 유일한 현장 대비책이다.
      파이에는 가상 키보드가 없어 화면 텍스트 입력만으로는 어르신이 쓸 수 없다.
      STT가 파이에서 통과하면 이 항목은 사라진다

### A 트랙 — 소프트웨어 (전부 로컬에서 가능)

- [ ] **팀원에게 알리기** — 우리 테이블 7개가 팀원의 `silvercare` DB에 생겼고, RDS 보안
      그룹에 규칙 2개를 추가했다. 팀원 테이블·데이터는 그대로인 것을 확인했다
- [ ] **보호자 앱 육안 확인** — 대화 로그·알림 이력이 보이는지 → **푸시 구독 다시 등록**
      (이관된 구독은 origin이 NULL이라 정리 로직이 지운다. 의도된 동작)
- [ ] **RDS 마스터 암호 교체** — **팀원 앱 설정도 함께 바꿔야 해 조율이 필요**하다.
      바꾼 뒤 로컬·EC2 양쪽 `.env`와 팀원 쪽을 갱신하고 `npm run verify-rds`로 확인한다.
      (암호 값은 이 레포에 적지 않는다.)
- [ ] **보호자 로그인(세션/JWT)** — 남은 것 중 **가장 큰 구조적 부채**. 여러 세션이 든다.
      `ROBOT_API_KEY`는 클라이언트 번들에 들어가므로 진짜 인증이 아니다. 시연용으로 수용한
      상태이며, 08-29 EC2 상시 배포로 **위험도가 올라갔다**
- [ ] **`purge-old-messages` 정기 실행 등록** — 삭제 범위는 확인 끝났다(`messages` 테이블
      하나뿐이라 **팀원 테이블에 닿지 않는다**). 남은 건 EC2에 cron/systemd timer를 다는 일

### B 트랙 — 하드웨어 (파이 현장 항목은 2순위에 있다)

- [ ] 파이 ↔ EC2 ↔ 보호자 앱 네트워크 구성 미정 (퍼블릭 도메인 vs 터널 유지).
      터널 유지 시 재시작마다 `set-url.sh` 한 번이 필요하다
- [ ] **YOLOv8 낙상 감지 실제 구현** — 백엔드 계약(`POST /api/detections`)과 mock이 이미
      완성돼 있어 **백엔드 변경은 불필요**하다. `detector/`에 Python(FastAPI) 서비스를 만들어
      계약대로 POST만 하면 된다. `docs/fall-detection.md` 참고. 배포 골격은 팀원 레포의
      `deploy/silvercare-api.service`를 그대로 쓸 만하다
      - [ ] **선행: detector용 presigned 업로드**(백로그). detector가 없는 지금 미리 만들면
            쓰이지 않는 코드가 되므로 A 트랙에 두지 않았다
      - [ ] 카메라 → detector 데이터 흐름 미정 (RTSP / HTTP 스트리밍 / 파일 감시)
      - [ ] 파이5 CPU 추론 가속(ONNX Runtime/OpenVINO) 여부와 목표 FPS 미정

> **낙상 감지에 GPU는 필요 없다** (08-31 판단). YOLOv8n은 GPU 없이 도는 것을 전제로 만든
> 모델이고, 파이5에서 입력을 320~416px로 낮추면 실용 FPS가 나온다 — **추론은 파이 로컬**.
> **Gemini로 낙상을 대체하는 것은 구조적으로 맞지 않는다** — 낙상은 하강 속도를 가진
> *움직임*인데 Gemini에 보내는 건 정지 프레임 한 장이라, 누운 사람이 "넘어진 것"인지
> "쉬는 것"인지 구분되지 않는다. → **YOLOv8n = 순간 이벤트(고빈도) / Gemini = 표정 요약(저빈도).**

---

## 프로젝트 제약 — 시작 전에 반드시 읽을 것

**대회(한이음 드림업) 제공 AWS 계정** (2026-08-27 실측 확정):

| 가능 | 불가능 |
|---|---|
| EC2, Lambda, RDS, DynamoDB, S3, API GW, Amplify, SQS, SNS | **Bedrock, Polly, Transcribe 등 클라우드 AI 전부** |

→ **AI는 Gemini API를 계속 쓴다.** 대화·비전·음성을 AWS로 옮기는 계획은 세우지 말 것.
Bedrock 어댑터를 실제로 만들어 테스트했으나 `BedrockDeny` 명시적 거부가 걸려 있었고,
explicit deny는 EC2 IAM Role로도 우회 불가라 **전부 제거했다**(2026-08-27).

- **Access Key 발급 절대 불가.** 인증은 IAM Role만(EC2는 `SafeInstanceProfile-{username}`)
  → **로컬에서는 AWS 인증 테스트가 불가능**하다. **예외: RDS**(사용자/비밀번호).
- MFA 재로그인 후에야 자원 생성 가능. 지정 리전 밖은 전부 차단 — **문제 생기면 리전부터.**
- EC2는 `t3.nano`~`t3.small`만. S3 버킷 이름은 본인 username으로 시작해야 함.
- 콘솔 **CloudShell**은 로그인 세션 자격증명을 쓰므로 Access Key 없이 CLI가 된다.

**남은 아키텍처 함정** — `services/history.js`에 **변환 계층이 아예 없다.** 저장 형식이 곧
Gemini 와이어 포맷(`{role, parts:[{text}]}`)이라 다른 LLM으로 갈아탈 일이 생기면 새로 만들어야
한다. 덤으로 model 턴 인코딩이 두 가지로 섞여 있다(실시간은 JSON, 재시작 복원은 평문).

---

## 운영 메모

**네트워크가 바뀌면 EC2·RDS가 통째로 막힌다** — 22번과 RDS 모두 개발 PC 공인 IP `/32`
하나로만 열려 있다. 증상은 연결 거부가 아니라 **타임아웃**이다. `https://checkip.amazonaws.com`
으로 새 IP를 확인해 콘솔 **CloudShell**에서 보안 그룹에 넣거나, **EC2 Instance Connect**로
우회한다. 절차는 `docs/pi-runbook.md` §0-A.

**RDS** — 08-31에 EC2까지 PostgreSQL 전환 완료. `DB_DRIVER=sqlite`로 되돌리면 즉시 파일 DB로
복귀한다. **팀원과 같은 `silvercare` DB를 쓴다**(우리 7개 테이블 + 팀원 3개). EC2·RDS·팀원
EC2가 같은 VPC라 **보안 그룹 참조(source-group)로 열어야 한다** — 공인 IP 규칙은 EC2 경로에
적용되지 않는다. **로컬 `.env`도 pg를 본다** → 로컬 대화 테스트가 실제 RDS에 쌓인다.
**테스트는 `.env`와 무관하게 항상 SQLite로 돈다**(각 테스트가 `DB_DRIVER`를 직접 고정한다.
이 핀을 빼면 `retention.test.js`가 실제 RDS 메시지를 지운다).

**⚠️ 드라이버를 건드리면 `npm run verify-rds`를 반드시 돌릴 것** — `pg-driver.test.js`는
pg-mem으로 도는데, 변이 테스트로 확인한 결과 **트랜잭션 롤백**과 **COUNT/id 타입** 둘은
코드를 망가뜨려도 통과한다. 둘 다 실제 RDS에서 통과하는 것을 확인했다.

**EC2의 `TTS_PROVIDER=gemini`** (09-01 추가). 파이에서 브라우저 TTS가 무음이라 서버측으로
돌렸다. 그 줄은 원래 `.env`에 **없었다** — EC2를 새로 만들거나 `.env`를 갈아엎으면
**다시 넣어야 로봇이 말을 한다.**

**실측 지연** (quick tunnel 경유):
- 폰 `/guardian/send` → 파이 발화 **5초 안팎** (폴링 대기 1.25초 + 음성 생성 1~3초 + 전송).
  캐시된 문구는 생성 구간이 사라진다
- **받아쓰기**: 같은 3.6초 오디오가 **3.0 / 3.4 / 20.3 / 52.3초**. 잘 되면 3초, Gemini가
  붐비면 자릿수가 달라진다. 느린 쪽은 전부 503 재시도라 시한(`STT_TIMEOUT_MS`, 12초)을 넣었다
- **원격조종 왕복 206~227ms**, 정지 판정 210~230ms, 손 뗀 뒤 실제 정지 0.9초 안팎 (09-03)

**서버측 STT가 감수하는 것 둘** — ① **방 안의 모든 발화가 Gemini로 올라간다**(웨이크워드
판정이 받아쓰기 *뒤*라 게이트가 닫혀 있어도 호출된다. **할당량을 쓰는 주범**). ② 말이
**끝나야** 시작한다(침묵 0.9초 감지 → 업로드 → 받아쓰기).

**파이 실측값** — `KIOSK_ROTATE=180`(터치 좌표도 함께 회전 확인) / 출력 `UACDemoV1.0` ·
입력 `reSpeaker 4-Mic Array` / **`git config core.fileMode false`를 걸어 뒀다**(안 그러면
`chmod +x`가 로컬 수정으로 남아 다음 `git pull`을 막는다).

**배포된 앱의 알려진 한계 3가지** (상세는 `docs/deploy-ec2-production.md`):
① quick tunnel은 **SSE를 통과시키지 못한다**(응급 푸시는 FCM 직통이라 무사, 보호자 앱은
30초 폴백 폴링). ② **터널 주소가 재시작마다 바뀐다** → 브라우저 기준 다른 사이트라
**푸시 구독을 다시 해야 한다.** ③ `ROBOT_API_KEY`는 진짜 인증이 아니다.

**인스턴스 크기** `t3.small`(1.9Gi)면 충분하다. `t3.nano`는 `@aws-sdk/client-s3` 설치에서
OOM 위험. **stop/start하면 IP가 바뀐다**(reboot은 유지):

```bash
npm run access -- <새 퍼블릭 IP>     # 끌 때는: npm run access -- --stopped
```

**로컬에서 HTTPS로 폰 테스트할 때** — `npm run dev`가 아니라 **preview를 터널링**해야 한다
(서비스 워커는 프로덕션 빌드에서만 등록된다). `npm run build && npm run preview`(4173) 후
cloudflared로 터널 하나. preview가 `/api`를 3001로 프록시한다.

---

## 백로그

로드맵에 안 들어갔지만 언젠가 해야 할 것들.

- [ ] **Gemini 없는 스냅샷 경로** (09-02, Vision을 끄면서 생김). 보호자 앱의 "방 안 모습"이
      `/api/vision`에 실려 오는 사진에만 의존하는데, 프론트는 Vision이 꺼져 있으면 호출 자체를
      안 한다. 그래서 할당량을 아끼려고 Vision을 끄면 **보호자가 집 안을 볼 수단이 통째로
      사라진다.** 분석 없이 사진만 올리는 경로가 있으면 둘을 분리할 수 있다(표정 요약은
      저빈도, 사진은 상시). 원격조종을 화면 보면서 하려면 어차피 필요하다
- [ ] **키오스크의 `⬅️ 이동 중` 표시가 이동 시간을 못 따라간다** (09-02). 큐에 방향 전환마다
      한 줄만 넣으므로 5초를 계속 밀어도 표시는 1.5초 뒤에 사라진다. **로봇이 어르신 앞에서
      움직이는데 화면은 조용한 상태**가 된다. `GET /api/control/state`를 함께 보고 `moving`인
      동안 표시를 유지하는 쪽이 맞다. **09-03에 바퀴가 실제로 돌기 시작해 조건이 충족됐다** —
      "표시할 움직임이 없어서 미뤄 둔 것"이 더는 아니다
- [ ] **detector용 presigned 업로드** (08-30). 지금 계약(base64 data URI)은 12MB JSON 바디를
      Express로 통과시킨다. 팀원 `app/s3_service.py`의 presigned PUT 방식이 이 경우 더 낫다.
      **보호자 앱 경로에는 쓰지 말 것** — `/api/snapshots/:filename` 프록시 서빙은 LAN 키
      인증이 안 깨지게 일부러 그렇게 만든 것이다
- [ ] **팀원과 테이블 이름이 충돌할 위험** (08-31). 지금은 안 겹치지만(`medications` vs
      `medication_records`) 양쪽이 테이블을 추가하면 언제든 겹친다. `initDB()`가
      `CREATE TABLE IF NOT EXISTS`를 돌리므로 **먼저 만든 쪽 스키마가 조용히 이긴다.**
      PostgreSQL 스키마 분리(`hyodol.` 접두)를 검토할 것
- [ ] **EC2의 `data/hyodol.sqlite` 정리** (08-31). RDS 전환 후 백업으로 남겨 뒀다. 한동안
      문제없이 도는 것을 확인하면 지운다. 지금 지우면 되돌릴 곳이 없어진다

**하지 말 것**
- ⛔ **팀원 `requirements.txt`를 그대로 설치하지 말 것** (08-30). 오염된 venv의 `pip freeze`라
  FastAPI와 무관한 `agent-detector`, `detect-installer`, `fastar`가 섞여 있다. `detector/` venv는
  fastapi/uvicorn/ultralytics만 직접 명시할 것

---

## 완료 요약

상세는 `git log`와 각 문서에. 여기엔 **날짜와 한 줄**만.

| 날짜 | 항목 |
|---|---|
| 09-03 밤 | **TTS 무음을 줄이고, 남은 무음은 보이게 했다** — `synthesize()`에 재시도가 없어 503 한 번이면 그 문장이 영영 소리가 없었다. 할당량 소진은 재시도하지 않는다(통을 더 태우므로). 브라우저 TTS가 소리를 못 내면 말풍선이 그렇게 적는다. **EC2 배포 + `prewarm-tts` 완료** — 예열 중 실제로 503이 나서 **재시도가 그 문구를 살렸다**. 테스트 백엔드 132→140 / 프론트 112→115 |
| 09-03 | **로봇이 처음으로 실제로 움직였다** — 우노 펌웨어를 덤프해 프로토콜(`F/B/L/R/S`, 115200)을 알아내고 `motors.py`를 채웠다. **원격조종 지연 첫 실측**(왕복 206~227ms, 정지 판정 850→210ms). **VAD 관측 스위치**(`?vad=1`)로 임계값을 할당량 0건으로 확정. **화면이 800×480 가로가 아니라 720×1280 세로**인 것을 확인하고 얼굴을 자리에 맞게 키웠다. 결함 6건 수정 |
| 09-02 | **원격조종을 꾹 누르는 방식으로** — 정지가 "신호가 도착해서"가 아니라 **"신선한 이동 의도가 끊겨서"** 일어나게 바꿨다. 폰이 꺼져도·와이파이가 끊겨도·EC2가 죽어도 로봇은 선다 |
| 09-02 | **Gemini 무료 등급의 정체 파악** — 모델당 하루 20건. 대체 모델로 안 넘어가던 결함을 고쳐 20→40. EC2의 Vision을 껐다 |
| 09-02 | **서버측 STT** — 파이가 직접 녹음해 `POST /api/stt`로 보내고 Gemini가 받아쓴다(Chromium STT 실패 우회). 화면 회전 고정 + 말풍선 확대. 파이 실측에서 결함 6건 추가 수정 |
| 09-01 | **파이 5 실물 연동 §0~§7** — 자동실행·카메라·마이크 통과, 서버측 TTS로 발화 해결, STT 실패 확정. 결함 7건 |
| 08-31 | **파이 연동 사전 준비** — 키오스크가 실패를 화면에 드러내도록 하드닝, `move` ack 충돌 해소, `deploy/pi/` 자산 |
| 08-31 | **RDS 실연결 + EC2 전환 완료** — 보안 그룹, `silvercare` DB로 108건 이관, `verify-rds` 양쪽 통과 |
| 08-31 | **감정 이력** — 표정이 바뀔 때만 기록. 보호자 홈이 로봇 발화가 아니라 어르신 표정을 근거로 말한다 |
| 08-31 | 백로그 4건(스냅샷 `Content-Type`, `suppressedBy`, 푸시 구독 origin, 오프라인 안내) / 테스트 보강 / 테스트가 실제 RDS를 지울 수 있던 결함 수정 |
| 08-30 | **RDS PostgreSQL 이전** — 드라이버 스위치, `schema.pg.sql`, 이관·검증 스크립트 / **복약 관리** — 팀원 코드에서 복약만 흡수 |
| 08-29 | **EC2 상시 배포**(같은 오리진 + cloudflared + systemd) / **S3 스냅샷 실연결** / 프론트 테스트 환경 도입 / 리포지토리 전면 async 전환 / `ACCESS.html` |
| 08-28 | 알림 상세 화면 + 푸시 딥링크 / 커버리지 감사 |
| 08-27 | Phase 5(응급 푸시), Phase 7(원격조종) / Bedrock 어댑터 → 계정 미지원 확인 → 전면 제거 |
| 08-26 | Phase 0~4 (기반 재설계·대화 안정화·응급 감지·대화 로그·보호자 PWA) |
